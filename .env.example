# Environment Variables Configuration

# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control

# Node Environment (development, production)
NODE_ENV=development

# JWT Secret - REQUIRED in production
# Generate with: openssl rand -base64 32
# NEVER use the default value in production
JWT_SECRET=your-super-secret-jwt-key-here

# AWS Configuration
AWS_REGION=us-west-2
DYNAMODB_TABLE=LoquatUsers
PINS_TABLE=LoquatPins

# Stadia Maps API Key — required for map tiles
STADIA_API_KEY=19a96a61-043f-4346-9b40-235ff994a0a3

# CORS Configuration - Production only
# Comma-separated list of allowed origins
# Example: https://loquat.example.com,https://www.loquat.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Server Configuration
PORT=8080
